import { Checkbox } from './Checkbox';

export default {
  title: 'Atom/Checkbox',
  component: Checkbox,
};

export const Default = {
  args: {
    checked: false,
    error: false,
    disabled: false,
  }
}
